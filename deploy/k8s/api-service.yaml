apiVersion: v1
kind: Service
metadata:
  name: rides-api
  labels:
    app: rides-api
spec:
  selector:
    app: rides-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
